(function () {

  var virtual_machines = [$("#virtual_machine").val()];

  $.ajax({
    url: '<%= start_workspace_path(@workspace) %>',
    type: 'post',
    dataType: 'script',
    data: {
      '_method': 'put',
      'authenticity_token': '<%= form_authenticity_token %>',
      'virtual_machines': virtual_machines
    },
    success: function() {
      alert("TODO: Launch Shellinabox");
    },
    error: function(msg) {
      alert("Something was wrong");
    }
  });

}());
