<%= javascript_tag do %>
  $(window).load( function() {
    DesktopEditor.init({
      container: "container",
      width: 700,
      height: 600,
      callback: null
    });
  });
<% end %>

<div id="container"></div>
