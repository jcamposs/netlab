$('#dialog')
  .html("<%=j render 'netwidget' %>")
  .dialog({
    autoOpen: false,
    title: '<%= @scene.name %>',
    modal: true,
    open: function(event, ui) {
      initWidget();
    }
  });
$('#dialog').dialog('open');
