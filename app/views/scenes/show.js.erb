$('#dialog')
  .html("<%=j render 'netwidget' %>")
  .dialog({
    width: <%= @width %> + 30,
    height: <%= @height %> + 60,
    title: '<%= @scene.name %>',
    modal: true,
    open: function(event, ui) {
      initWidget();
    }
  });
$('#dialog').dialog('open');
